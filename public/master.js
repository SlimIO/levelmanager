document.addEventListener("DOMContentLoaded", function domReady() {
    console.log("DOM Loaded!");

    const btn_add_db = document.getElementById("btn_add_db");
    const btn_del_db = document.getElementById("btn_add_db");
});
